## In `editor\`
This folder only contains the extensions, types and instance.

To add a new extension, the developer should add a new `.ts` file in `extensions\` folder.

Second, the developer should name the file end with upper case `E`, like `TableE.ts` *(This for a reminder of developer this is an extension)*

Then, the developer should connect the extension to the `extensions.ts`
