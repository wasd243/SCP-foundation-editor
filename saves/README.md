# Saves

This folder contains all the saves from the editor.

Currently, it only saves the `.ftml` source because the wiki themes are not supported yet.

In this repository, the `saves\` folder only contains some example `.ftml` saves to keep the folder.
