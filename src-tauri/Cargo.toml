[package]
name = "scpeditor-bin"
version = "0.1.0"
edition = "2021"
authors = ["wasd243"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Parser
w_parser = { path = "../crates/w_parser" }

# Exporter is not implemented yet
# w_exporter = { path = "../crates/w_exporter" }
