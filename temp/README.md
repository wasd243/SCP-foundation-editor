# temp

This is not a trash folder.

SCP Editor uses this directory as a runtime workspace for parser, exporter, and
merger intermediate files.

Generated files in this folder may change often and are intentionally ignored by
Git. Keep this directory, `README.md`, and `.gitkeep` in the repository.

Do not delete this folder unless you are also changing the runtime path logic.
